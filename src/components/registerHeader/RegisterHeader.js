export default function RegisterHeader() {
  return (
    <h2
      style={{
        color: 'var(--brand-color)',
        marginTop: 0,
        marginBottom: 90,
        fontSize: 32,
        fontWeight: 700,
        lineHeight: 2.1,
      }}
    >
      Sign Up
    </h2>
  );
}
